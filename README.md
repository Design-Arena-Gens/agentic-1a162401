# বাংলা শুভেচ্ছা অভিজ্ঞতা

বাংলা ভাষায় আন্তরিকভাবে শুভেচ্ছা জানাতে সাহায্য করার জন্য একটি ক্ষুদ্র Next.js অ্যাপ। পেজে প্রবেশ করলেই একটি সুন্দর বাংলা শুভেচ্ছা দেখা যায় এবং বোতামে ক্লিক করলেই নতুন শুভেচ্ছা যুক্ত হয়।

## 🚀 লোকালি চালানো

```bash
npm install
npm run dev
```

ব্রাউজারে `http://localhost:3000` খুললেই অ্যাপ দেখা যাবে।

## 🧱 স্ট্যাক

- Next.js 14 (App Router)
- React 18
- গুগল ফন্ট `Noto Sans Bengali`

## 🔍 স্ক্রিপ্ট

- `npm run dev` – ডেভ সার্ভার চালু করে
- `npm run build` – প্রোডাকশন বিল্ড তৈরি করে
- `npm start` – প্রোডাকশন সার্ভার চালু করে
- `npm run lint` – Next.js লিন্ট চালায়

## 📦 ডিপ্লয়মেন্ট

ভেরসেলে ডিপ্লয় করতে চালান:

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-1a162401
```
